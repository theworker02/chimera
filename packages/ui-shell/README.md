# ui-shell

Glassmorphic MeshShell workspace (static HTML/JS/CSS).

**Status:** working — served by the Chimera management API at `/meshshell`.

Canonical assets: `web/` (synced from `meshshell/web/`).
