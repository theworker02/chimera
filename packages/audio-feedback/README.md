# audio-feedback

Procedural sound synthesis for Chimera system events.

**Status:** working — PCM/WAV generators in `synth.py` and browser WebAudio helper `webaudio.js`.

## Events
- `peer_join`, `peer_lost`, `task_complete`, `alert`
